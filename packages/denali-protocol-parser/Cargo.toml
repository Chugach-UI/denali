[package]
name = "denali-protocol-parser"
version = "0.1.0"
edition = "2024"

[dependencies]
itertools = "0.15.0"
serde = { version = "1.0.229", features = ["derive"] }
quick-xml = { version = "0.42.0", features = ["serialize"] }

[lints]
workspace = true
